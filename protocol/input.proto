syntax = "proto3";
package miniarena;

// 说明：为便于快速跑通，本项目线上消息采用 JSON。
// 该 proto 仅作为未来扩展的协议定义示例。

message InputMessage {
  string type = 1;    // 固定为 "move"
  string command = 2; // up / down / left / right
}

